= form_for @tv_show do |f|
  - if @tv_show.errors.any?
    #error_explanation
      %h2= "#{pluralize(@tv_show.errors.count, "error")} prohibited this tv_show from being saved:"
      %ul
        - @tv_show.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    = f.submit 'Save'
